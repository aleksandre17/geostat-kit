{
  "description": "Default keys for compose generation and ops scripts. Override in ops/config/deploy.env.",
  "keys": {
    "DEPLOY_PROJECT": "from_repo_folder",
    "COMPOSE_PROJECT_NAME": "from_repo_folder",
    "COMPOSE_API_SERVICE": "{slug}-api",
    "COMPOSE_APP_SERVICE": "{slug}-app",
    "DOCKER_NETWORK": "{slug}-net",
    "API_PORT": "8090",
    "WORKER_PORT": "8091",
    "DEPLOY_HOST_PORT": "5177",
    "APP_DEV_CONTAINER_PORT": "5177",
    "OPS_BUILD_TMP_PREFIX": "ops-build"
  }
}
